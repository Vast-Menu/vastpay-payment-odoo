<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="payment_vastpay.VastPayQRDialog">
        <Dialog>
            <t t-set-slot="header">
                <h4 class="modal-title flex-grow-1">VastPay Payment</h4>
                <button type="button" class="btn-close" aria-label="Close"
                        title="Close (does not cancel the payment)"
                        t-on-click="closeOnly"/>
            </t>
            <div class="d-flex flex-column align-items-center text-center gap-2 p-2">
                <p class="mb-0">Scan the QR code to pay <strong t-esc="props.amountLabel"/></p>
                <img t-att-src="props.qrImage" alt="VastPay"
                     style="width:260px;height:260px;"/>
                <small class="text-muted">Waiting for payment confirmation…</small>
                <a t-att-href="props.paymentUrl" target="_blank" class="small">
                    Open payment page
                </a>
                <t t-if="state.message">
                    <small class="text-danger" t-esc="state.message"/>
                </t>
            </div>
            <t t-set-slot="footer">
                <button class="btn btn-secondary" t-on-click="closeOnly">Close</button>
                <button class="btn btn-primary"
                        t-att-disabled="state.checking" t-on-click="check">
                    <t t-if="state.checking">Checking…</t>
                    <t t-else="">Check payment</t>
                </button>
            </t>
        </Dialog>
    </t>

</templates>
